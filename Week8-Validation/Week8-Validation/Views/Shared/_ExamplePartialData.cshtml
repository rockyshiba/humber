@model IEnumerable<Week8_Validation.Models.Student>

<h4>Here are the list of students from the database</h4>
<ul>
    @foreach(var item in Model)
    {
        <li>@item.First_name @item.Last_name</li>
    }
</ul>
@DateTime.Now