@model PracticeMidterm.Models.ProgramFaculty

@{
    ViewBag.Title = "Details";
}

<h2>Details</h2>

<div>
    <h4>Program</h4>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.program.Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.program.Name)
        </dd>

    </dl>
    <h4>Faculty associated with Program</h4>
    <ul>
    @foreach(var item in Model.faculties)
    {
        <li>@item.First_name, @item.Last_name</li>
    }
    </ul>
</div>
<p>
    @Html.ActionLink("Edit", "Edit", new { id = Model.program.Id }) |
    @Html.ActionLink("Back to List", "Index")
</p>
